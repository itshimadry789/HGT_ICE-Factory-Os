/**
 * Factory Configuration Constants
 * These values represent operational benchmarks for the ice factory.
 * TODO: Move to a settings table in the future for dynamic configuration.
 */

export const MAX_DAILY_PRODUCTION_CAPACITY = 1000; // blocks per day
export const TARGET_FUEL_EFFICIENCY = 4.0; // blocks per liter
export const TARGET_COST_PER_UNIT = 45.0; // currency units (SSP)

